// project import
import sandbox from './sandbox';
import template from './utilities';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [sandbox, template]
};

export default menuItems;
